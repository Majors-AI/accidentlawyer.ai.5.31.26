// Placeholder — built in a later step (Accounting department).
import DepartmentSection from './DepartmentSection';

export default function Accounting() {
  return <DepartmentSection deptId="accounting" />;
}
